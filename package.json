{
  "name": "gateon",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "npm run lint:web && npm run lint:api",
    "lint:web": "biome check apps/web",
    "lint:api": "npm run lint -w @gateon/api",
    "format": "npm run format:web && npm run format:api",
    "format:web": "biome format --write apps/web",
    "format:api": "npm run format -w @gateon/api",
    "dev:web": "turbo dev --filter=@gateon/web",
    "dev:api": "turbo dev --filter=@gateon/api",
    "build:web": "turbo build --filter=@gateon/web",
    "build:api": "turbo build --filter=@gateon/api"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "turbo": "^2.5.3"
  }
}
